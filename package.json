{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
  "build": "pnpm install --recursive && pnpm --filter @workspace/faed-marketplace build",
  "typecheck:libs": "tsc --build",
  "typecheck": "pnpm run typecheck:libs && pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.4",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}